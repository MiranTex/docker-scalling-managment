# Provisionamento automático do datasource: o Grafana lê isto ao subir e já
# fica com o Prometheus configurado, sem precisar clicar em nada na UI.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
